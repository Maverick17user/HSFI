import { REMOVE_SHEDULE_UNIT_FROM_VENREG } from '../types'

export const removeShaduleObject = (target, index, prop) => dispatch => {
    console.log('uhuu');
    
    // dispatch(removeShaduleObjectFromVenReg(target, index, prop));  
}

// export const removeShaduleObjectFromVenReg = (target, index, prop) => {
//     return {
//         type: REMOVE_SHEDULE_UNIT_FROM_VENREG,
//         target,
//         index,
//         prop
//     }
// }